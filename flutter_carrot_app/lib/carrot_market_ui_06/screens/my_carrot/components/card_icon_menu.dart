import 'package:flutter/material.dart';

class CardIconMenu extends StatelessWidget {
  const CardIconMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
